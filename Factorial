class Solution {
    // Function to calculate factorial of a number.
    int factorial(int n) {
        // code here
        if(n==0)
        return 1;
        int a=1;
        for(int i=2;i<=n;i++){
            a=a*i;
            
        }
        return a;
    }
}